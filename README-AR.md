# وصلة - نظام إدارة شبكات الإنترنت

نظام متكامل لإدارة شبكات الإنترنت، يشمل إدارة المشتركين، الراوترات، المبيعات، الموظفين، والتقارير.

---

## 📋 المتطلبات

قبل البدء، تأكد من تثبيت:

- **Node.js** (الإصدار 18 أو أحدث)
  - [تحميل Node.js](https://nodejs.org/ar)
  - اختر "LTS" (الإصدار المستقر)

---

## 🚀 التثبيت (مرة واحدة فقط)

### الخطوة 1: تحميل المشروع

```bash
git clone https://github.com/اسم-المستخدم/اسم-المشروع.git
cd اسم-المشروع
```

### الخطوة 2: تثبيت المتطلبات

افتح موجه الأوامر (Command Prompt) في مجلد المشروع واكتب:

```bash
npm install
```

ثم انتقل لمجلد الخادم وثبّت متطلباته:

```bash
cd server
npm install
cd ..
```

---

## ▶️ تشغيل النظام

### الطريقة السهلة (موصى بها)

#### على Windows:
انقر نقرتين على ملف `start-windows.bat`

#### على Mac/Linux:
```bash
chmod +x start-mac-linux.sh
./start-mac-linux.sh
```

سيفتح المتصفح تلقائياً وسيظهر لك عنوان IP للدخول من الجوال.

---

### الطريقة اليدوية

إذا لم تعمل الطريقة السهلة، اتبع هذه الخطوات:

#### 1. تشغيل الخادم (نافذة أوامر جديدة):
```bash
cd server
npm start
```
انتظر حتى ترى: `✅ الخادم يعمل على المنفذ 3001`

#### 2. تشغيل الواجهة (نافذة أوامر جديدة):
```bash
npm run dev -- --host
```
انتظر حتى ترى الرابط: `http://localhost:5173`

#### 3. افتح المتصفح:
- من اللابتوب: `http://localhost:5173`
- من الجوال: `http://[عنوان-IP]:5173`

---

## 📱 الوصول من الجوال

1. تأكد أن الجوال واللابتوب على **نفس شبكة الواي فاي**
2. اعرف عنوان IP للابتوب:
   - **Windows**: افتح CMD واكتب `ipconfig` (ابحث عن IPv4)
   - **Mac**: افتح Terminal واكتب `ipconfig getifaddr en0`
3. من متصفح الجوال، ادخل: `http://[عنوان-IP]:5173`
   - مثال: `http://192.168.1.100:5173`

---

## ⚙️ إعداد المزامنة

لمزامنة البيانات بين الأجهزة:

1. سجّل دخول بـ: `admin` / `admin123`
2. اذهب إلى **الإعدادات** ← **إعدادات الخادم**
3. فعّل "استخدام الخادم المحلي"
4. أدخل عنوان الخادم: `http://[عنوان-IP]:3001`
5. اضغط **اختبار الاتصال** ثم **حفظ**
6. كرر نفس الخطوات على الجوال

---

## 🔐 بيانات الدخول الافتراضية

| اسم المستخدم | كلمة المرور |
|-------------|-------------|
| admin       | admin123    |

⚠️ **مهم**: غيّر كلمة المرور بعد أول تسجيل دخول!

---

## 📁 هيكل المشروع

```
المشروع/
├── src/                    # ملفات الواجهة الأمامية
├── server/                 # ملفات الخادم
│   ├── server.js          # الخادم الرئيسي
│   └── wasla_database.json # قاعدة البيانات
├── start-windows.bat       # تشغيل على Windows
├── start-mac-linux.sh      # تشغيل على Mac/Linux
└── README-AR.md           # هذا الملف
```

---

## 🔧 حل المشاكل الشائعة

### المشكلة: "npm غير معروف"
**الحل**: تأكد من تثبيت Node.js وأعد تشغيل موجه الأوامر.

### المشكلة: "فشل الاتصال بالخادم"
**الحل**:
1. تأكد أن الخادم يعمل (ترى رسالة "الخادم يعمل على المنفذ 3001")
2. تأكد أن عنوان IP صحيح
3. تأكد أن الأجهزة على نفس الشبكة
4. جرب تعطيل جدار الحماية مؤقتاً

### المشكلة: "المنفذ مستخدم"
**الحل**: أغلق أي تطبيق يستخدم المنفذ 3001 أو 5173، أو أعد تشغيل الجهاز.

### المشكلة: "البيانات لا تتزامن"
**الحل**:
1. تأكد من تفعيل "استخدام الخادم المحلي" على **جميع** الأجهزة
2. تأكد أن عنوان الخادم نفسه على جميع الأجهزة
3. اضغط اختبار الاتصال وتأكد أنه "متصل"

---

## 📞 الدعم

للمساعدة أو الإبلاغ عن مشاكل، تواصل معنا.

---

## 📄 الترخيص

جميع الحقوق محفوظة © 2024

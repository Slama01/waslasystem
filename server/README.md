# خادم نظام وصلة المحلي

## متطلبات التشغيل
- Node.js (الإصدار 18 أو أحدث)

## خطوات التشغيل

### 1. تثبيت Node.js
حمّل Node.js **الإصدار LTS** من الرابط التالي وثبّته:
https://nodejs.org/

⚠️ **مهم**: اختر إصدار **LTS** (الإصدار المستقر) وليس Current

### 2. حذف مجلد node_modules القديم (إذا وجد)
احذف مجلد `node_modules` يدوياً من مجلد `server` إذا كان موجوداً

### 3. تثبيت المكتبات
افتح موجه الأوامر (Command Prompt) في مجلد `server` ونفّذ:
```bash
npm install
```

### 4. تشغيل الخادم
```bash
npm start
```

### 5. ستظهر رسالة مثل:
```
========================================
   نظام وصلة - الخادم المحلي
========================================

✅ الخادم يعمل على المنفذ 3001

📡 روابط الوصول:
   - محلي: http://localhost:3001

🔒 الخادم يعمل على localhost فقط (آمن)
   لتفعيل الوصول من الشبكة: LOCAL_SERVER_NETWORK=true npm start
========================================
```

### 6. تفعيل الوصول من الشبكة (اختياري)
للسماح بالوصول من أجهزة أخرى على الشبكة:

**Windows:**
```bash
set LOCAL_SERVER_NETWORK=true && npm start
```

**Mac/Linux:**
```bash
LOCAL_SERVER_NETWORK=true npm start
```

### 7. فتح التطبيق
- من الكمبيوتر: افتح `http://localhost:5173` بعد تشغيل الواجهة
- من الجوال: افتح رابط الشبكة (مثل `http://192.168.1.100:5173`)

## تشغيل الواجهة الأمامية
في مجلد المشروع الرئيسي (ليس server):
```bash
npm install
npm run dev -- --host
```

## ملاحظات مهمة
- تأكد أن الكمبيوتر والجوال على نفس شبكة WiFi
- قد تحتاج لفتح المنفذ 3001 و 5173 في جدار الحماية
- البيانات محفوظة في ملف `wasla_database.json`

## 🔒 الأمان

### تشفير كلمات المرور
- جميع كلمات المرور مشفرة باستخدام bcrypt
- كلمات المرور القديمة تُحدّث تلقائياً عند تسجيل الدخول

### حماية API
- جميع نقاط API محمية بنظام جلسات (Session Tokens)
- يجب تسجيل الدخول للوصول للبيانات
- نقاط النسخ الاحتياطي والاستعادة للمدير فقط

### الوصول من الشبكة
- افتراضياً، الخادم يعمل على localhost فقط (آمن)
- لتفعيل الوصول من الشبكة، استخدم متغير البيئة `LOCAL_SERVER_NETWORK=true`

## بيانات الدخول الافتراضية
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`

⚠️ **مهم**: غيّر كلمة المرور الافتراضية فوراً بعد أول تسجيل دخول!

## النسخ الاحتياطي
- النسخ الاحتياطي متاح فقط للمدراء المسجلين
- كلمات المرور لا تُصدّر في النسخ الاحتياطي لأسباب أمنية
- لعمل نسخة احتياطية يدوية: انسخ ملف `wasla_database.json`
